services:
  - type: web
    name: raksha-pratibha-sangram
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
